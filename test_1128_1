import java.util.Scanner;

public class test_1128_1 {
	public static void main(String[] args) {
		/*
		//1.	1~10까지 합을 구해라. (for, while, do~while)
		//for
		int sum=0, i=1;
		
		for(i=1; i<11; i++) {
			sum+=i;
		}
		
		//while1
		while(true) {
			sum+=i;
			i++;
			if(i>10) {
				break;
			}
		}
			
		//while2
		while(i<=10) {
			sum+=i;
			i++;
		}	
		
		
		//do_while
		do {
			sum+=i;
			i++;
		}while(i<=10);
		System.out.println(sum);
		
		
		//2.	while문을 이용하여 정수를 여러 개 입력받고 평균 출력해라. (0이 입력되면 입력이 종료된다.)

		Scanner s=new Scanner(System.in);
		int sum=0; 
		int cnt=0;
		
		while(true) {
			System.out.println("정수 입력: ");
			int n=s.nextInt(); //정수 입력	//3 2
			if(n==0) {
				break;
			}
			cnt++;
			sum+=n;
		}
		System.out.println((double)(sum)/cnt);
		
		
		
		//3.	다음 while문을 보고 실행결과가 동일하게 나오도록 2가지 과정으로 바꿔라.
		//0부터 10까지의 짝수들의 누적합
		int i=0, sum=0;
		while(i<10){
		i=i+2;
		sum += i;	//0+2+4+6+8+10
		}
		System.out.println(sum);	//30

		//3.1) while(true)와 break를 사용하여 바꿔라.
		int i=0, sum=0;
		while(true) {
			if(i>=10) {
				break;
			}
			i=i+2;
			sum+=i;
		}
		System.out.println(sum);	//30
	
		//3.2) do~while문, continue을 사용하여 바꿔라.
		int i=0, sum=0;
		do {
			i=i+2;	//0+2+4+6+8+10
			if(i%2==1) {	//홀수일떄는 밑(if문 밖)의 코드 실행안됨
			//ㅑ%2!=0도 맞다
				continue;	//제외
			}
			sum+=i;	//
		}while(i<10);
		
		System.out.println(sum);	//30
		
		
	
	//4-1) 10개의 문자를 가지는 배열 a를 선언하고 생성해라.
		char a[]=new char[10];
		for(int i=0; i<a.length; i++) {
			a[i]='a';
			System.out.println(a[i]);
		}
	//4-2) 0~4까지 정수형으로 초기화된 배열 b를 선언하고 생성해라.
		int b[]=new int[] {01,2,3,4};
		
	
	//4-3) “java”, “c”, “c++”로 초기화된 배열 c를 선언하고 생성해라.
		String c[]=new String[] {"java","c","c++"};
		
	//4-4) 인덱스의 최대값이 9인 정수형 배열 d를 선언하고 생성해라.
		int d[]=new int[10];
	
		
		*/
	}
}
