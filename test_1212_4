import java.util.Scanner;
/*
class Profile {
	String name, id;

	Profile(String name, String id) {
		this.name = name;
		this.id = id;
	}

	String getname() {
		return name;
	}

	String getid() {
		return id;
	}
}

public class test_1212_4 {
	public static void main(String[] args) {
		// 3. 다음 class를 보고 코드를 작성해라.
		// class Profile{
		// String name,id;
		// Profile(String name, String id){
		// this.name=name; this.id=id;
		// }
		// String getname(){ return name;}
		// String getid(){ return id;}
		// }
		// A. main() Prob1: main()에서 인원수를 입력받아 인원수를 Profile에 대한 객체배열 개수로 설정한다.
		// 이름과 아이디도 입력받아 객체배열에 저장한후 출력해라.
		Scanner s = new Scanner(System.in);
		System.out.print("인원수를 입력 : ");
		int a = s.nextInt();

		Profile[] pro = new Profile[a];
		for (int i = 0; i < pro.length; i++) {
			System.out.println("이름과 id 입력 : ");
			String name = s.next();
			String id = s.next();
			pro[i] = new Profile(name, id);
		}
		s.close();
		for (int i = 0; i < pro.length; i++) {
			System.out.println(pro[i].getname() + " " + pro[i].getid());
		}
	}
}
*/

