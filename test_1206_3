import java.util.Scanner;
/*
class XY {
	int a;
	int b;

	void set(int a, int b) {
		this.a = a;
		this.b = b;
	}

	void show() {
		System.out.println(a + "," + b);
	}
}

class XYZ extends XY {
	String color;

	void color(String color) {
		this.color = color;
	}

	void show() {
		super.show();
		System.out.println(color);
	}
}

public class test_1206_3 {
	public static void main(String[] args) {
		// 4. Main()를 보고 클래스 구현해라
		// (XY는 슈퍼클래스, XYZ는 서브클래스)
		// main(){
		// XY x= new XY();
		// x.set(1,2);
		// x.show(); //1,2 출력
		// XYZ xy=new XYZ();
		// xy.set(4,5);
		// xy.color(“blue”);
		// xy.show(); //4,5,blue 출력
		XY x = new XY();
		x.set(1, 2);
		x.show();

		XYZ xy = new XYZ();
		xy.set(4, 5);
		xy.color("blue");
		xy.show();

		// 5. 두 사람이 가위바위보를 한다.
		// 가위,바위,보 중 하나를 문자열로 입력받아 누가 이겼는지 판단하는 프로그램을작성해라.
		// (중첩 if문 사용해야함, 문자열 비교)
		// 실행결과) 영수 : 가위
		// 철수 : 보
		// 철수가 이겼다.
		Scanner s = new Scanner(System.in);

		System.out.println("영수 : ");
		String str1 = s.next();

		System.out.println("철수 : ");
		String str2 = s.next();

		if (str1.equals("가위")) {
			if (str2.equals("가위")) {
				System.out.println("비겼다.");
			} else if (str2.equals("바위")) {
				System.out.println("철수가 이겼다.");
			} else if (str2.equals("보")) {
				System.out.println("영수가 이겼다.");
			} else {
				System.out.println("잘못된 값");
			}
		} else if (str1.equals("바위")) {
			if (str2.equals("가위")) {
				System.out.println("영수가 이겼다");
			} else if (str2.equals("바위")) {
				System.out.println("비겼다.");
			} else if (str2.equals("보")) {
				System.out.println("철수가 이겼다.");
			} else {
				System.out.println("잘못된 값");
			}

		} else if (str1.equals("보")) {
			if (str2.equals("가위")) {
				System.out.println("철수가 이겼다");
			} else if (str2.equals("바위")) {
				System.out.println("영수가 이겼다");
			} else if (str2.equals("보")) {
				System.out.println("비겼다");
			} else {
				System.out.println("잘못된 값");
			}
		} else {
			System.out.println("잘못된 값");
		}
		s.close();
	}
}
*/
