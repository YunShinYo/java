
public class Ex_Socket1 {

	public static void main(String[] args) {
		//TCP - 일일이 확인을 해야함(신뢰) 
		// 속도가 느리다
		// 예)이메일(수신확인), FTP, 웹(HTTP) 등
		//UDP - 비신뢰적
		// 속도가 더 빠르다
		// 동영상, 게임 등등에 사용
		
		
		//Client(사용자)	Server(서버)
		//자바에서 소켓 통신할 수 있는 라이브러리 지원(자바로 많이 이용)
		//소켓 종류:서버 소켓과 클라이언트 소켓
		
		//소스짤때 2가지 입장
		//1. 서버입장(서비스제공함)
		//2. 클라이언트입장(서비스제공받음)
		
		//클라이언트	  소켓필요(IP주소, 포트번호)							서버
		//(나)														(친구)
		//															1. (연결)할 서버소켓 생성
		//소켓이 필요함 -> 아이피주소와 포트번호를 연결해야하기 때문)				연결요청오면 연결수락(기다라야된다)
		//			안녕(getOutputStream()<->getInputStream())		2. 메시지를 받을 소켓 생성
		
		//-클라이언트는 그냥 소켓이고 일반 소켓인데
		//-서버는 서버 소켓임
		
		//java.net패키지에 포함
		
		//Socker(InetAddress address, int port)
		//			->ip주소이다
		
		//-주요메소드
		//bind()
		//close()
		//connect()
		//getOutputStream()
		//isConnected()
		
		//서버소켓에 사용되는 클래스
		//ServerSocket()생성자
		//accept(); 메소드 -> 수락
		
		
		
		
		
		
		
		
		
		
		
		
	}
}
