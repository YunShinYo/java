import java.util.Scanner;

public class test_1130_3 {
	static void func(int a, int b) {
		Scanner s=new Scanner(System.in);
		System.out.print("입력: ");
		int n=s.nextInt();
		
		a=n/5;	//몫
		b=n%5;	//나머지
		
		System.out.println("a: "+a+", b: "+b);
		}
	public static void main(String[] args) {
		/*
		//5. 무한루프안에 두 정수를 입력받아 합을 구하고, 실수를 입력하면 “실수는 안된다”라고 출력하고, 
		//다시 두 정수를 입력받는 코드를 작성해라.
		//실행 결과) 두 정수 입력: 3 3.5
		//실수는 안된다
		//두 정수 입력: 3 5
		//답: 8
		
		Scanner s= new Scanner(System.in);
		int a=0, b=0, sum=0;
		while(true) {
			try {
				System.out.print("두 정수 입력: ");
				a=s.nextInt();
				b=s.nextInt();
				System.out.print("답: "+(a+b)+"\n");	//괄호없이는 문자열로 인식하기때문에 (a+b)로 묶어줘야한다.
				break;
			}
			catch(Exception e){
				//e.printStackTrace();
				s.next();	//토큰 지우는 코드=입력했던거 지운다->★없으면 무한루프...
				//System.out.println("실수는 안된다");
				continue;
			}
		}
		*/
		
		//6. func함수 작성하기.(키보드로 입력한 양수를 5로 나누어 몫과 나머지를 변수에 저장하기)
		//main(){
		//func(a, b); //a->몫, b-> 나머지
		//}
		int a=0, b=0;
		func(a, b);
	
		

	}
}
